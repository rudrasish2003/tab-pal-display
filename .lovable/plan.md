# Interactive AI Email Dashboard

## Build
- Replace the blank page with a responsive banking operations dashboard closely matching the supplied reference: navy sidebar, pale workspace, dense KPI cards, charts, tables, and status indicators.
- Add a shared app shell with collapsible desktop navigation and a mobile drawer.
- Create working views for Overview, Email Queue, AI Performance, Customer Insights, Service Categories, SLA & Operations, Risk & Compliance, Reports, and Settings.
- Populate every view with realistic in-memory sample data; no account, database, or external service is required.

## Interactions
- Make date-range, status, priority, and category filters update visible sample results.
- Support selecting queue rows, opening an email detail panel, resolving/escalating items, and searching the queue.
- Make chart range controls, report downloads, quick actions, settings toggles, and sidebar collapse controls respond visibly.
- Add useful empty states and confirmation messages where appropriate.

## Visual and Technical Details
- Use the existing TanStack routing, shared UI controls, Recharts, and Lucide icons.
- Define a compact blue/green/red banking design system with semantic color tokens and accessible contrast.
- Keep each navigation destination as its own page with unique page metadata.
- Verify desktop and mobile layouts, all primary interactions, and the final preview build.
